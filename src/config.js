export const DASHBOARD_PATH = '/dashboard';
export const DEFAULT_THEME_MODE = 'dark';  //default dark

export const CSS_VAR_PREFIX = '';

const config = {
  fontFamily: `'Poppins','Roboto', 'Noto Sans Arabic', sans-serif`,
  borderRadius: 8,
  direction: 'ltr',  //default direction
  mode: 'light',
  locale: 'en'
};

export default config;
